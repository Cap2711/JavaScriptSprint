// commands/init.js
const fs = require('fs');
const path = require('path');

module.exports = {
  command: 'init',
  describe: 'Initialize the application',
  handler: function () {
   //initilize the application
  }
};
